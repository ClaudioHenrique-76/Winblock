#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    //Entrada
    float Preco, Unidade, Reajuste;
    float total a pagar, total a pagar com descont, desconto;
    int quantidade;
    printf("Mercado:");
    printf("Dados da compra:");
    printf("Preco do produto: %f");
    scanf("%f", Preco);
    printf("Quantidade: %i");
    scanf("%i", Unidade);
    printf("Desconto: %i");
    scanf("%i", Reajuste);

    //processamento
    TAP = Preco *
    return 0;
}
